# Requirements for Sugarscape experiments with vLLM
# Install with: pip install -r requirements-sugarscape.txt
#
# For development, also install the package:
#   pip install -e .

# Core dependencies (also in pyproject.toml)
pyyaml>=6.0

# LLM Provider - OpenAI client (used for vLLM's OpenAI-compatible API)
openai>=1.0.0

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# HuggingFace Hub for model downloads
huggingface_hub>=0.20.0

# vLLM for local inference (install separately if running the server)
# Note: vLLM requires specific CUDA versions - see https://docs.vllm.ai/en/latest/getting_started/installation.html
# Uncomment the line below if you're running the vLLM server on this machine:
# vllm>=0.4.0
